# Phase 4: Enhanced Memory & Context - Additional Dependencies

# Semantic Search (Feature 4.1)
# NOTE: torch is installed as a separate step in the Dockerfile using the CPU-only
# PyTorch index (https://download.pytorch.org/whl/cpu) to avoid the ~4.3GB
# NVIDIA/CUDA/triton cascade. Embeddings run on CPU via asyncio.to_thread().
chromadb>=0.4.0
sentence-transformers>=2.2.0

# Already installed (verify):
# fastapi>=0.104.0
# sqlalchemy>=2.0.0
# anthropic>=0.7.0
# openai>=1.0.0
