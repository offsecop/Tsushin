# Development & CI Testing Dependencies
# NOT installed in the production Docker image.
# Install locally or in CI: pip install -r backend/requirements-dev.txt

pytest>=7.4.4
pytest-asyncio>=0.23.3
pytest-cov>=4.1.0
