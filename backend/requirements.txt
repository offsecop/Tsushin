# Core Framework
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.6  # Required for file uploads

# Database
sqlalchemy>=2.0.25
psycopg2-binary>=2.9.9
alembic>=1.13.0

# Pydantic (flexible for MCP compatibility)
pydantic>=2.7.0
pydantic-settings>=2.5.2

# Environment
python-dotenv>=1.0.0

# AI Providers
anthropic>=0.18.1  # Phase 6.11.1: Async support
openai>=1.40.0  # Phase 5.0: Audio skills
google-generativeai>=0.3.2
google-genai>=1.0.0  # Image generation with Gemini 2.0 Flash

# HTTP Clients
httpx>=0.27.0  # Updated for MCP compatibility
requests>=2.31.0

# Utilities
watchdog>=4.0.0
beautifulsoup4>=4.12.3
Pillow>=10.0.0  # Image processing for ImageSkill
dateparser>=1.2.0
pytz>=2024.1
cachetools>=5.3.0  # TTL cache for Hub services
json-repair>=0.25.0  # Fix malformed JSON from LLM responses

# Security
PyJWT>=2.8.0
argon2-cffi>=25.1.0
email-validator>=2.3.0
cryptography>=41.0.0  # Hub security (Fernet encryption)
slowapi>=0.1.9  # MED-004: Rate limiting for auth endpoints

# Docker & MCP
docker>=7.0.0  # Phase 8: Multi-tenant MCP containerization
mcp>=1.0.0  # Model Context Protocol client

# Kubernetes (optional — only needed when TSN_CONTAINER_RUNTIME=kubernetes)
# kubernetes>=28.1.0

# GCP Secret Manager (optional — only needed when TSN_SECRET_BACKEND=gcp)
# google-cloud-secret-manager>=2.18.0

# Browser Automation (Phase 14.5)
playwright>=1.48.0  # Browser automation for web tasks
aiohttp>=3.8.0  # Phase 8: MCP HTTP bridge communication

# Telegram Integration (Phase 10.1.1)
python-telegram-bot>=21.0

# Observability
prometheus_client>=0.20.0  # Prometheus metrics exposition

# Testing
pytest>=7.4.4
pytest-asyncio>=0.23.3
