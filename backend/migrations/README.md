# Legacy SQLite Migration Scripts

These migration scripts apply ONLY to the SQLite fallback path.

PostgreSQL schema is managed exclusively by Alembic (`backend/alembic/`).
No new files should be added to this directory.

These scripts are preserved for:
- SQLite dev/local fallback mode
- Historical reference
